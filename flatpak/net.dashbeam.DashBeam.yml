id: net.dashbeam.DashBeam
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
  - org.freedesktop.Sdk.Extension.node22
command: dashbeam

finish-args:
  - --share=ipc
  - --share=network
  - --socket=wayland
  - --socket=fallback-x11
  - --device=dri
  # See: https://docs.flathub.org/linter#finish-args-home-filesystem-access
  - --filesystem=home
  - --talk-name=org.kde.StatusNotifierWatcher
  - --talk-name=org.freedesktop.Notifications

build-options:
  append-path: /usr/lib/sdk/rust-stable/bin:/usr/lib/sdk/node22/bin
  env:
    CARGO_HOME: /run/build/dashbeam/cargo

modules:
  - name: dashbeam
    buildsystem: simple
    sources:
      - type: git
        url: https://github.com/tonyantony300/dashbeam.git
        # Bump both to the release tag cut after this PR merges. v0.6.1 and
        # earlier predate the flatpak/ dir and the wasm decoupling, so the build
        # fails against them. Regenerate the vendored sources at the same time.
        tag: v0.5.0
        commit: 36a6c66eabb0baca1595f0a3c319f044f73fcd33
      - cargo-sources.json
      - node-sources.json
      - type: archive
        url: https://registry.npmjs.org/pnpm/-/pnpm-10.12.1.tgz
        sha256: 889bac470ec93ccc3764488a19d6ba8f9c648ad5e50a9a6e4be3768a5de387a3
        dest: flatpak-node/pnpm
        strip-components: 1
    build-options:
      env:
        VITE_IS_FLATPAK: 'true'
    build-commands:
      - node flatpak-node/pnpm/bin/pnpm.cjs install --offline --frozen-lockfile
      - node flatpak-node/pnpm/bin/pnpm.cjs run build
      - node flatpak-node/pnpm/bin/pnpm.cjs exec tauri build --no-bundle -c src-tauri/tauri.linux-flatpak.conf.json
      # The binary keeps the cargo package name; productName does not rename it.
      - install -Dm755 src-tauri/target/release/alt-sendme /app/bin/dashbeam
      - install -Dm644 flatpak/net.dashbeam.DashBeam.desktop /app/share/applications/net.dashbeam.DashBeam.desktop
      - install -Dm644 flatpak/net.dashbeam.DashBeam.metainfo.xml /app/share/metainfo/net.dashbeam.DashBeam.metainfo.xml
      - install -Dm644 flatpak/icons/128x128.png /app/share/icons/hicolor/128x128/apps/net.dashbeam.DashBeam.png
      - install -Dm644 flatpak/icons/512x512.png /app/share/icons/hicolor/512x512/apps/net.dashbeam.DashBeam.png
