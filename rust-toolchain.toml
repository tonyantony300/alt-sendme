# Pin Rust version for reproducible builds (CI, F-Droid, local).
# Must match src-tauri/Cargo.toml [package] rust-version and docs/fdroid-metadata-example.yml sudo block.
[toolchain]
channel = "1.81"
