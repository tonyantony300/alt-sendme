# Optional: with direnv installed, entering this directory enters the dev shell.
#   direnv allow
# Without direnv, just run `nix develop` instead.
#
# nix-direnv makes this fast by keeping the shell cached across `cd`s:
#   https://github.com/nix-community/nix-direnv
use flake
