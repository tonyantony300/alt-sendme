{
  "name": "AltSendme",
  "version": "0.2.0",
  "scripts": {
    "dev": "cd ../web-app && npm run dev",
    "build": "cd ../web-app && npm run build",
    "sync-version": "node ../scripts/sync-version.js"
  }
}
